def anagrams(input)
=begin
  input_array = input.split(" ")
  puts "==================== input array ==========================="
  puts input_array

  # list_of_lists = %i(a c).map { |x| input_array.map { |e| e[x] } }
  list_of_lists = %i(e[0]).map { |x| input_array.map { |e| e[x] } }
  puts "======================= list of lists ================================="
  puts list_of_lists

  input_array
=end
end